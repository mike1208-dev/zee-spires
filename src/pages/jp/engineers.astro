---
import Layout from "@/layouts/Layout.astro";
import EngineersContent from "@/components/EngineersContent.astro";
import { resolveLocale, useTranslations } from "@/i18n/utils";

const t = useTranslations(resolveLocale(Astro.currentLocale));
---

<Layout title={t("engineersPage.title")} description={t("engineersPage.metaDescription")}>
  <EngineersContent />
</Layout>
