# Custom 404 handling for /jp/* paths is done in functions/_middleware.ts
# instead of here — Cloudflare Pages' _redirects only accepts 200/301/302/
# 303/307/308 as rewrite status codes, so a "404" status rule (which was
# here before) is invalid and silently does nothing.
